/* ============================================================
   Entry point — loaded LAST, after every module has defined its
   functions and registered its handlers. Kicks off the app.
   ============================================================ */
init();
